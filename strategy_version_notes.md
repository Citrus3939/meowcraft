# TradingView 策略版本变化说明

本文档记录 `macd_stochastic_rr_strategy.pine` 在多次调整后的主要变化，方便后续回测时对照参数和行为。

## 当前策略概览

策略核心仍然是：

- MACD 柱线 `delta` 上穿/下穿 0。
- Stochastic 的 `%K`、`%D` 金叉/死叉。
- 当前版本不再使用前高/前低止损，改为入场价反向 `d%` 止损。
- 价格正向波动达到 `2d%` 时减仓，并将剩余仓位止损移动到保本。
- BTC 5分钟高盈亏比默认开启 EMA200 顺势过滤，核心 MACD+Stochastic 入场公式不变。
- 剩余仓位增加最终止盈，避免大幅浮盈回落到保本。

## 版本演进

| 版本 | 对应提交 | 主题 |
| --- | --- | --- |
| v1 | `0b814ef` | 增加前高/前低止损、1:1 减仓保本 |
| v2 | `27cfa25` | 删除 `calc_on_order_fills`，降低前瞻性偏差风险 |
| v3 | `bf68693` | 增加风险仓位、仓位上限和止损距离过滤 |
| v4 | `757dd5c` | 增加剩余仓位最终止盈 |
| v5 | `21e3af5` | 改为 `d%` 百分比止损和 `2d%` 减仓 |
| v6 | `9144e7c` | BTC 5分钟高盈亏比参数和 EMA 顺势过滤 |

### v1 - 初始增强版

目标：在原 MACD + Stochastic 策略上增加前高/前低止损和 1:1 减仓保本。

主要功能：

- 做多止损：入场前 `lookback_high` 周期最低点减去 ATR 缓冲。
- 做空止损：入场前 `lookback_high` 周期最高点加上 ATR 缓冲。
- 达到 1R 后按 `partial_close_ratio` 减仓。
- 减仓后剩余仓位止损移动到开仓均价。
- 绘制初始止损线、1:1 目标线、保本线和开仓信号。

### v2 - 去除前瞻性偏差风险

问题：早期版本使用了 `calc_on_order_fills=true`，TradingView 官方文档提示该参数可能让策略在历史 K 线内成交后重新计算，从而产生过于乐观的回测结果。

调整：

- 删除 `calc_on_order_fills=true`。
- 不使用 `request.security(..., lookahead_on)`。
- 不使用 `security()` 跨周期读取未来数据。
- 前高/前低使用 `[1]`：
  - `ta.lowest(low, lookback_high)[1]`
  - `ta.highest(high, lookback_high)[1]`

效果：

- 止损依据只来自已经完成的历史 K 线。
- 回测结果更保守，更接近实际交易逻辑。

### v3 - 减少 Margin Call 和无效交易

问题：满仓开仓时，如果前高/前低止损距离过大，或者遇到跳空，回测会出现大量 `Margin Call`，造成无效强平和手续费损耗。

调整：

- 将默认仓位模式从 `100%` 权益改为固定数量模式。
- 每次下单使用显式 `qty`，按风险反推仓位。
- 新增风控参数：
  - `单笔最大风险占权益(%)`
  - `最大仓位占权益(%)`
  - `最大止损距离(%)`
  - `最小下单数量`
- 设置：
  - `margin_long=100`
  - `margin_short=100`
- 如果实际成交后止损距离超过限制，会关闭该无效风控仓位。

效果：

- 避免每笔交易默认满仓。
- 跳过止损距离过大的信号。
- 降低 Margin Call 和无意义手续费交易的概率。

### v4 - 增加剩余仓位最终止盈

问题：1R 减仓后，剩余仓位只有保本止损，没有最终止盈。价格即使浮盈 5%、10%，也可能回落到保本退出，导致剩余仓位收益显示为 0。

调整：

- 新增 `启用剩余仓位止盈`。
- 新增 `剩余仓位止盈模式`：
  - `RR`
  - `百分比`
  - `较近者`
- 新增：
  - `剩余仓位止盈RR`
  - `剩余仓位止盈百分比(%)`
- 默认使用 `较近者`，即在 RR 止盈和固定百分比止盈之间选择更近的目标。
- 减仓后剩余仓位同时挂：
  - 保本止损
  - 最终止盈

效果：

- 防止盈利头寸大幅回撤到保本。
- 可以直接设置 5%、8%、10% 等固定盈利目标。
- 也可以继续使用 RR 目标，如 2R、3R。

### v5 - 改为 d% 百分比止损和 2d% 减仓

问题：前高/前低止损会让止损距离随行情结构大幅变化，配合最大止损距离过滤后，订单触发数量偏少；同时过远或不稳定的止损距离容易导致交易经常打损。

调整：

- 删除当前交易逻辑里的前高/前低止损。
- 删除 ATR 缓冲止损。
- 删除 `最大止损距离(%)` 过滤，改由 `d止损百分比(%)` 直接控制风险距离。
- 新增/改用 `d止损百分比(%)`：
  - 做多止损 = 入场均价 * `(1 - d%)`。
  - 做空止损 = 入场均价 * `(1 + d%)`。
- 2d 减仓：
  - 做多减仓价 = 入场均价 * `(1 + 2d%)`。
  - 做空减仓价 = 入场均价 * `(1 - 2d%)`。
- 仓位计算改为按 `d%` 风险距离反推下单数量。
- 剩余仓位最终止盈仍保留，但默认 RR 从 2R 调整为 4R，避免和 2d 减仓价重叠。

效果：

- 不再因为前高/前低距离过远而过滤大量信号。
- 每笔交易的止损距离更稳定。
- 正向达到 2d 时先锁定部分利润，剩余仓位再保本或最终止盈。

### v6 - BTC 5分钟高盈亏比调优

用户设定：BTC、5分钟周期、`d=1%`、保留做空、目标是提高盈亏比并接受较低胜率。

调整：

- 保留原 MACD + Stochastic 核心入场公式：
  - 多单核心信号仍是 `%K/%D` 金叉、`k < OverSold`、MACD 柱线上穿 0。
  - 空单核心信号仍是 `%K/%D` 死叉、`k > OverBought`、MACD 柱线下穿 0。
- 新增默认开启的 EMA 顺势过滤：
  - 多单需要 `close > EMA200` 且 EMA200 向上。
  - 空单需要 `close < EMA200` 且 EMA200 向下。
  - 关闭 `启用EMA顺势过滤` 后，可恢复原核心入场信号。
- 将 `2d` 减仓比例默认从 50% 降到 25%，让更多仓位保留给后续趋势。
- 将剩余仓位止盈模式默认改为 `RR`。
- 将剩余仓位止盈 RR 默认从 4R 提高到 6R。
- 将剩余仓位止盈百分比默认从 8% 提高到 10%。

效果预期：

- 交易次数会减少。
- 胜率可能继续偏低，但单笔盈利交易的潜在盈亏比更高。
- 减少逆势抄底和摸顶，保留多空双向顺势交易。

## 当前关键参数建议

### 风控参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| d止损百分比(%) | 1.0 | 价格反向波动超过 d% 止损；调大可减少被小波动扫损，调小可降低单笔风险距离 |
| 单笔最大风险占权益(%) | 1.0 | 每笔按 d% 止损距离计算仓位，建议 0.5 到 2 |
| 最大仓位占权益(%) | 25.0 | 限制单笔名义仓位，建议 10 到 30 |
| 最小下单数量 | 0.0 | 股票可设为 1，加密货币可按交易所最小数量设置 |

### 趋势过滤参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 启用EMA顺势过滤 | true | BTC 5分钟默认开启；关闭后恢复原核心入场信号 |
| 趋势EMA长度 | 200 | 多单在 EMA 上方且 EMA 上行，空单在 EMA 下方且 EMA 下行 |
| 趋势斜率确认K数 | 5 | EMA 与 N 根前比较，用于确认趋势方向 |

### 减仓和止盈参数

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 启用2d减仓保本 | true | 达到正向 2d% 后减仓并移动剩余仓位止损到保本 |
| 减仓比例(%) | 25 | 2d 位置平掉的仓位比例；默认少减仓以提高盈亏比 |
| 启用剩余仓位止盈 | true | 给剩余仓位增加最终止盈 |
| 剩余仓位止盈模式 | RR | BTC 5分钟高盈亏比默认使用更远 RR 目标 |
| 剩余仓位止盈RR | 6.0 | R 等于 d% 初始风险，默认 6R 即约 6d% |
| 剩余仓位止盈百分比(%) | 10.0 | 按入场均价计算固定百分比止盈 |

## 回测注意事项

- 建议在普通 K 线图上回测，不建议在 Renko、Kagi 等非标准图表上判断策略表现。
- 不要开启会让策略在历史 K 线内重复计算的设置，否则结果可能偏乐观。
- 如果仍然有大量 Margin Call：
  - 降低 `最大仓位占权益(%)`。
  - 降低 `单笔最大风险占权益(%)`。
  - 适当降低 `d止损百分比(%)`。
- 如果订单触发多但经常被小波动扫损：
  - 适当提高 `d止损百分比(%)`。
  - 同时降低 `单笔最大风险占权益(%)`，避免总风险变大。
  - 保持 `启用EMA顺势过滤` 开启，减少逆势交易。
- 如果交易太少：
  - 关闭 `启用EMA顺势过滤`，检查是否是过滤过严。
  - 或降低 `趋势EMA长度`，例如从 200 降到 100。
- 如果经常浮盈后回到保本：
  - 降低 `剩余仓位止盈百分比(%)`。
  - 或降低 `剩余仓位止盈RR`。
  - 或把止盈模式改为 `百分比`。

## 当前版本核心出场逻辑

做多：

1. 初始止损 = 入场均价 * `(1 - d%)`。
2. 正向达到 `2d%` 位置减仓。
3. 减仓后剩余仓位止损 = 开仓均价。
4. 剩余仓位到达最终止盈目标时平仓。

做空：

1. 初始止损 = 入场均价 * `(1 + d%)`。
2. 正向达到 `2d%` 位置减仓。
3. 减仓后剩余仓位止损 = 开仓均价。
4. 剩余仓位到达最终止盈目标时平仓。
