Place demo assets here:

- `miku-avatar.png`: transparent PNG silhouette used for word target sampling.
- `finished-wig.png`: transparent PNG or JPG of the final wig reveal.

The component also includes a procedural fallback shape, so the demo still runs before these assets are added.
